	    if (!againing && inputdir>=0) {
			if (suppressInput) {
				//suppressInput = false;
				//goToPauseScreen();
			} else if (inputdir===4 && ('noaction' in state.metadata)) {

            } else {
                pushInput(inputdir);
                if (processInput(inputdir)) {
                    redraw();
                }
	        }
	       	return prevent(e);
    	}

// into

		if (inputdir>=0) {
			if (suppressInput) {
			} else if (inputdir===4 && ('noaction' in state.metadata)) {
			} else {
				pushInput(inputdir);
				while (againing) {
					processInput(-1); // edit for nuworm to allow skipping again
				}
				if (processInput(inputdir)) {
					redraw();
				}
			}
			return prevent(e);
		}
