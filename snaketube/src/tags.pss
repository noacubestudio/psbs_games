dirs = up down left right (logical/ visual directions of objects, usually for rotated sprite)
offs = up down left right (used as temporary visual offsets for animation, can coexist with dirs)
hor = left right
ver = up down

x = 0 1 (bool)

( notUp = left down right )
( notLeft = up down right )
( notDown = up left right )
( notRight = up left down )

( MAPPINGS )

( dirs => otherdirs )
( up down left right -> notUp notLeft notDown notRight )